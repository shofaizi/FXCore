<p>FX page</p>
<ol>
  <% for(var key in data) {%>
    <% if(!data.hasOwnProperty(key)) continue; %>
    <% var obj = data[key]; %>
      <% for(var prop in obj) {%>
        <% if(!obj.hasOwnProperty(prop)) continue; %>
      <% } %>
      <li><%= key%> : <%= obj %></li>
  <% } %>
</ol>
